class Pt(x: Int, y: Int) {
    this.x = x;
    this.y = y;

     def PLUS(other: Pt):Pt {
         return Pt(this.x+other.x, this.y+other.y);
     }
}

// instantiate a Pt explicitly & then implicitly
a: Pt = Pt(4,5);
b = Pt(1,2);

// c & d will be equivalent
c = a.PLUS(b);
d = a + b;
